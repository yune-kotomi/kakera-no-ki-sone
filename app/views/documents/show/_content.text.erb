<%= "#{parent_index}#{content_counter + 1}. #{content['title']}" %>
<%= content['body'] %>
<%= "\n" + render(:partial => 'documents/show/content',
  :collection => content['children'],
  :locals => {:parent_index => "#{parent_index}#{content_counter + 1}."}) if content['children'].present? %>
